export default function wordLengths(total){
    let sumForAll = total.split(" ")
    let result3 = 0;
      for(var i=0; i<sumForAll.length; i++){
        result3 += sumForAll[i].length
       // return result.length
        
    }console.log(result3)
      return result3
    }
    